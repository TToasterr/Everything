def cmd(message, msg):
  global doMsgOut, msgOut, consoleOut, channel, msgAmm

  doMsgOut = True
  msgOut = [msg[6:]]
  consoleOut = "%s made the bot say '%s'" % (message.author, msg[6:])
  channel = [message.channel]
  msgAmm = 1
